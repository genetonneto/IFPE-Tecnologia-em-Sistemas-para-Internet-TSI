// Implementar um subprograma que dado um vetor, inverte a ordem do seu conteúdo Exemplo – Entrada: [1, 3, 5, -2], Saída: [-2, 5, 3, 1];
 
function inverso(vetor) {
    for (i = 0; i < vetor.length; i++) {
    }
    return vetor.reverse();
}

let vetor = [1,3,5,-2];
let resultado = inverso(vetor);
console.log(resultado);